<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- RED -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <!-- BACKGROUND / WAKELOCK -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        REQUEST_IGNORE_BATTERY_OPTIMIZATIONS: permite pedirle al usuario
        que excluya la app del optimizador de batería. Google puede pedir
        justificación en Play Store — la razón válida es "nodo de red P2P
        soberano que necesita mantener conexiones cifradas activas".
        Si Play Store lo rechaza, sacá este permiso y documentalo.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- NOTIFICACIONES (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="XionChat"
        android:name=".XionApplication"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Foreground service nativo en Kotlin: mantiene vivo el proceso
             Go (libxionia.so) en background con PARTIAL_WAKE_LOCK.
             foregroundServiceType="connectedDevice" es el tipo correcto
             para un nodo de red P2P — no requiere justificación adicional
             en Play Store a diferencia de "dataSync" o "location". -->
        <service
            android:name=".XioniaService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
